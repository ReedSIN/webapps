#Django settings for webapps project.

DEBUG = True
TEMPLATE_DEBUG = DEBUG

ADMINS = (
	('Jacob Menick','jmenick@reed.edu'), ('Brett Beutell', 'bbeutell@reed.edu'), ('Tom LaMarre', 'tlamarre@reed.edu'), ( 'Lizzi Lindboe', 'lindbel@reed.edu'), ('Erica Edmondson, eedmonds@reed.edu'),
)

MANAGERS = ADMINS
'''
## BB
## Django 1.6 upgrade
## Kept old settings in case you decide to install an earlier version again
DATABASES = {
    'default': {
        'ENGINE' : 'django.db.backends.mysql',
        'NAME' : 'webapps',
        'USER' : 'webapps',
        'PASSWORD' : 'SiS1@3$',
        'HOST' : '',
        'PORT' : '',
        }
}
'''
## BB
## Old settings
DATABASE_ENGINE = 'mysql'           # 'postgresql_psycopg2', 'postgresql', 'mysql', 'sqlite3' or 'ado_mssql'.
DATABASE_OPTIONS = {"init_command": "SET storage_engine=INNODB"} # MSK 6/30/10
DATABASE_NAME = 'webapps'             # Or path to database file if using sqlite3.
DATABASE_USER = 'webapps'             # Not used with sqlite3.
DATABASE_PASSWORD = 'SiS1@3$'         # Not used with sqlite3.
DATABASE_HOST = ''             # Set to empty string for localhost. Not used with sqlite3.
DATABASE_PORT = ''             # Set to empty string for default. Not used with sqlite3.

# Local time zone for this installation. Choices can be found here:
# http://www.postgresql.org/docs/8.1/static/datetime-keywords.html#DATETIME-TIMEZONE-SET-TABLE
# although not all variations may be possible on all operating systems.
# If running in a Windows environment this must be set to the same as your
# system time zone.
TIME_ZONE = 'US/Pacific'

# Language code for this installation. All choices can be found here:
# http://www.w3.org/TR/REC-html40/struct/dirlang.html#langcodes
# http://blogs.law.harvard.edu/tech/stories/storyReader$15
LANGUAGE_CODE = 'en-us'

SITE_ID = 1

# If you set this to False, Django will make some optimizations so as not
# to load the internationalization machinery.
USE_I18N = True

# Absolute path to the directory that holds media.
# Example: "/home/media/media.lawrence.com/"
MEDIA_ROOT = ''

# URL that handles the media served from MEDIA_ROOT.
# Example: "http://media.lawrence.com"
MEDIA_URL = ''

# URL prefix for admin media -- CSS, JavaScript and images. Make sure to use a
# trailing slash.
# Examples: "http://foo.com/media/", "/media/".
ADMIN_MEDIA_PREFIX = '/media/'

# Make this unique, and don't share it with anybody.
SECRET_KEY = '9chlo-a#d@=)$=v2la(e*c-w+^a@z-p1!%(jvq*58qf*_r$26h'

EMAIL_HOST = 'localhost'
EMAIL_HOST_USER = ''
EMAIL_HOST_PASSWORD = ''
SERVER_EMAIL = 'noreply@sin.reed.edu'
EMAIL_USE_TLS = False

# List of callables that know how to import templates from various sources.
TEMPLATE_LOADERS = (
    'django.template.loaders.filesystem.Loader',
    'django.template.loaders.app_directories.Loader',
#     'django.template.loaders.eggs.load_template_source',
)

#import py_cosign.cosign as py_cosign
#COSIGN_CONFIG = py_cosign.COSIGN_CONFIG

MIDDLEWARE_CLASSES = (
    'django.middleware.common.CommonMiddleware', 
    'django.contrib.sessions.middleware.SessionMiddleware', ## MK
    'django.contrib.auth.middleware.AuthenticationMiddleware', ## MK
#    'django.contrib.auth.middleware.RemoteUserMiddleware',   ## BB
#    'django.contrib.messages.middleware.MessageMiddleware', ## BB
    'django.middleware.doc.XViewMiddleware',
    'webapps.middleware.cosign.middleware.CosignAuthenticationMiddleware',
)

### For future upgrades
## BB
'''
AUTHENTICATION_BACKENDS = (
  #    'django.contrib.auth.backends.RemoteUserBackend',  ## BB
        'webapps.generic.auth_backend.SinUserModelBackend', ## BB
)

AUTH_USER_MODEL = 'generic.SinUser'
'''

ROOT_URLCONF = 'webapps.urls'

TEMPLATE_DIRS = (
   '/var/django/webapps/templates/'
)

INSTALLED_APPS = (
    'django.contrib.contenttypes',
    'django.contrib.sites',
    'django.contrib.admin', ## MK
    'django.contrib.sessions', ## MK
    'django.contrib.auth', ## MK
    'django.contrib.contenttypes', ## MK
    'webapps.finance',
    'webapps.appointments',
    'webapps.generic',
    'webapps.users',
    'webapps.organizations',
    'webapps.fundingpoll',
    'webapps.forms',
    'webapps.paideia',
    'webapps.home',        #BB
    'webapps.classifieds', #JM
    'webapps.VotW', ## BB
    'webapps.wiki', ## BB
    'webapps.elections',
    'sortedm2m', # JM - a further attempt at getting the voteArray to work. 
)

TEMPLATE_CONTEXT_PROCESSORS = ( ## MK 8/4/11: useful additions to context for templates e.g. user info
    'django.contrib.auth.context_processors.auth',
)
